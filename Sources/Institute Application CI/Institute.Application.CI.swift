public import Institute_Model

extension Institute.Application {
    /// The continuous-integration command family: the typed `institute ci`
    /// verbs that operate the reabsorbed `Institute.CI` domain — gitignore
    /// canon rendering and validation, control-workspace validation, package
    /// posture, and bootstrap manifest verification.
    public enum CI {}
}
