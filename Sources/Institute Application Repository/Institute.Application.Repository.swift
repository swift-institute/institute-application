public import Institute_Model

extension Institute.Application {
    /// The repository command family: the typed `institute repository` verbs
    /// that operate the `Institute.Repository.Policy` domain — fleet census,
    /// caller waves, and uniformity waves — through the application's
    /// GitHub seam.
    public enum Repository {}
}
